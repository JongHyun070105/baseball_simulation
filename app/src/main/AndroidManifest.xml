<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.baseball_simulation_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.WhatIfBaseball">
    <activity
        android:name=".MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <!-- 추후 추가될 다른 액티비티들 -->
    <activity
        android:name=".ui.main.HighlightActivity"
        android:exported="false" />
    <activity android:name=".ui.main.ChangeMemberActivity"
        android:exported="false" />
    <activity
        android:name=".ui.main.MyPickActivity"
        android:exported="false" />
    <activity
        android:name=".ui.main.ResultActivity"
        android:exported="false" />
</application>

</manifest>